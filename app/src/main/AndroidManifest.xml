<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.drops.c3p0.andorlegendenapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_andor_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_andor_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ListViewActivityLegends"
            android:label="@string/title_activity_legends"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity
            android:name=".activity.LegendActivity"
            android:label="LegendActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <!--  <activity
              android:name=".controller.DbLegends"
              android:label="DbLegends"/>
          <activity
              android:name=".beans.Legend"
              android:label="Legend"/>
  -->

    </application>

</manifest>